FROM node:24-alpine

# Use reliable mirror for Alpine packages
RUN sed -i 's/dl-cdn.alpinelinux.org/mirrors.aliyun.com/g' /etc/apk/repositories

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# Create .env file if it doesn't exist (environment variables are set via docker-compose)
RUN touch .env || true

EXPOSE 3000

CMD ["npm", "run", "dev"]
